name: Crosspost
type: module
description: 'Shares content to social platforms once its public address answers, and keeps a record per post and platform.'
package: Social
core_version_requirement: ^10.3 || ^11
configure: crosspost.connections
dependencies:
  - drupal:node
  - key:key
